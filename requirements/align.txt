# AlignScore evaluation environment
#
# Setup:
#   conda create -n eval_align python=3.11 -y
#   conda activate eval_align
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements-align.txt
#   pip install --no-deps "git+https://github.com/yuh-zha/AlignScore.git"
#   python -m spacy download en_core_web_sm
#   mkdir -p models/
#   wget https://huggingface.co/yzha/AlignScore/resolve/main/AlignScore-large.ckpt -P models/
#
# Verify:
#   python -c "from alignscore import AlignScore; print('OK')"

# Versions pinned for AlignScore 0.1.3 compatibility
transformers==4.40.0           # still exports AdamW (deprecated but present)
pytorch-lightning==1.9.5       # old load_from_checkpoint API alignscore expects
huggingface-hub>=0.19.3,<1.0
tokenizers
safetensors

# NLP tools
spacy>=3.0.0
scikit-learn
sentencepiece
protobuf>=3.20.0
jsonlines
nltk>=3.8.0

# Shared deps (needed by run_evaluation.py)
pyyaml>=6.0
tqdm>=4.65.0
numpy<2.0
scipy<1.14
