# Main environment: generation + completeness evaluation
# Install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
#   uv pip install -r requirements.txt
#
# NOTE: PyTorch cu130 phải cài TRƯỚC requirements.txt vì cu130 wheel
#       không tồn tại trên PyPI mặc định.
# CUDA 13.0 + Driver 580 → sm_120 (Blackwell) native support.
#
# Flash Attention 3 (install separately AFTER PyTorch):
#   pip install flash_attn_3 --find-links https://windreamer.github.io/flash-attention3-wheels/cu130_torch2110

# LLM Inference (HuggingFace Transformers)

--extra-index-url https://pypi.org/simple
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0         # 8-bit quantization (all models)
sentencepiece>=0.1.99        # Tokenizer for Mistral/BioMistral
protobuf>=3.20.0             # Required by sentencepiece
# Flash Attention 3 — installed separately (see header)
# Qwen3.5 DeltaNet fast path (CUDA 13.0 compatible via Triton)
flash-linear-attention
# NOTE: causal-conv1d NOT needed — fla provides Triton-based conv1d.
#       Qwen3.5 runs via Ollama (bundles its own runtime), not Transformers.

# Data processing
numpy<=1.26.4            # Pin: spacy/thinc incompatible with numpy 2.x
pandas>=2.0.0
datasets>=2.14.0

# Evaluation — Completeness (P vs L)
rouge-score>=0.1.2
bert-score>=0.3.13
sacrebleu>=2.3.0
scispacy>=0.5.0
# SciSpaCy model (install separately):
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_lg-0.5.4.tar.gz

# Config & utilities
pyyaml>=6.0
tqdm>=4.65.0
tenacity>=8.0.0
requests>=2.28.0
packaging>=23.0
psutil>=5.9.0

# Testing
pytest>=7.4.0

# Notebooks
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Download
gdown>=5.0.0
